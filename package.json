{
  "name": "presentations-root",
  "private": true,
  "workspaces": [
    "vendor/SyncDeck-Reveal"
  ],
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "stage-site": "node scripts/stage-site.mjs",
    "check-css": "node scripts/check-css-coverage.mjs",
    "test": "npm run test --workspaces --if-present"
  }
}
